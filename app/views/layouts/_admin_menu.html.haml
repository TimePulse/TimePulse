- if current_user and current_user.admin?
  - items = ["Users", users_path], ["Groups", groups_path], ["Permissions", permissions_path], ["Clients", clients_path], ["Projects", projects_path], ["Bills", bills_path ], ["Invoices", invoices_path]
  .nav#admin_nav
    %div
      %ul
        -items.each do |name, path|
          %li= link_to(name, path)
