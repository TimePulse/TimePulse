- if current_user and current_user.admin?
  - items = ["Users", users_path], ["Groups", groups_path], ["Permissions", permissions_path]
  .nav#admin_nav
    %div
      %ul
        -items.each do |name, path|
          -if authorized_url?(path)
            %li= link_to(name, path)
