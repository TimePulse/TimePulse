- set_headline "Dashboard: ".html_safe + short_name_with_client(current_project)

-content_for :aux do
  = render :partial => "shared/timeclock"
  = render :partial => "shared/recent_work"

-content_for :picker do
  = render :partial => "shared/project_picker"

= render :partial => 'home/current_project_block'
