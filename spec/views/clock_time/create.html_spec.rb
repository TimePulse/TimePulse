require 'spec_helper'

describe "/clock_time/create" do
  before(:each) do
    assign(:work_unit, Factory(:work_unit))
    assign(:project, Factory(:project))
    render
  end

  it "should succeed" do
      
  end
end
